# Configuration SMTP pour tests locaux
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=nom-utilisateur@example.com
SMTP_PASS=mot-de-passe-ou-app-password

# Adresse email de destination DIGI
EMAIL_DIGI=adv@exemple.com

# Origines autorisees pour le CORS (liste separée par virgule)
ALLOWED_ORIGINS=http://localhost:8000
